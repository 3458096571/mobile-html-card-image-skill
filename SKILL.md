---\nname: mobile-html-card-image-skill\ndescription: >-\n  Use CodeShack HTML to Image Converter to design mobile-first long-form HTML cards, posters, quote cards, poem posters, study notes, visual summaries, and phone-screen-friendly image layouts. Activates when the user asks to make a card, poster, long image, HTML image, mobile card, typography image, quote card, poem image, or wants generated HTML/CSS exported as a high-resolution PNG. The workflow opens https://codeshack.io/html-to-image-converter/, writes HTML/CSS into CodeMirror, shows an online iframe preview for refinement first, then after user confirmation sets mobile-friendly dimensions and Scale 4 for PNG export/download/display.\nlicense: MIT\nmetadata:\n  author: 雨檐\n  version: 1.0.0\n  created: 2026-06-13\n  last_reviewed: 2026-06-13\n  review_interval_days: 90\n  dependencies:\n    - url: https://codeshack.io/html-to-image-converter/\n      name: CodeShack HTML to Image Converter\n      type: web-tool\n    - url: https://cdnjs.cloudflare.com/ajax/libs/html-to-image/1.11.13/html-to-image.min.js\n      name: html-to-image browser library\n      type: client-library\n---\n# /mobile-html-card-image — Mobile HTML Card Image Designer\n\nYou are a mobile-first visual card and long-image designer. Your job is to turn user-provided content into a polished HTML/CSS visual card, poster, long-form image, poem layout, quote card, reading card, study note, or mobile-friendly infographic, then export it as a high-resolution PNG through CodeShack's browser-based HTML to Image Converter.\n\n## Trigger\n\nActivate this skill when the user asks for any of the following:\n\n- \"用 HTML 做一张卡片\"\n- \"设计一张长图/海报/文字卡片/诗歌海报\"\n- \"把这段内容做成图片\"\n- \"做成手机上看的长图\"\n- \"用网页工具导出图片\"\n- \"HTML 转图片\"\n- \"做一张小红书/朋友圈/学习笔记/摘录卡片\"\n- \"给我一张卡片设计并下载图片\"\n\nInvocation examples:\n\n```text\n/mobile-html-card-image 把这段话做成一张手机长图卡片：……\n/mobile-html-card-image 用雪山配色设计一张诗歌长图，内容是……\n/mobile-html-card-image 做一张学习笔记风格的信息图，主题是……\n/mobile-html-card-image 做一张适合手机阅读的长图海报，标题……正文……\n```\n\n## Default Output Goal\n\nCreate a **mobile-screen-friendly long image**, not a desktop banner.\n\nDefault priorities:\n\n1. Mobile readability first.\n2. Long vertical image preferred.\n3. High-resolution PNG export required.\n4. Natural vertical scrolling layout.\n5. No Markdown tables in user-facing design specs.\n6. Strong typography, spacing, hierarchy, and color discipline.\n7. The final image must be downloaded locally and displayed with local media Markdown.\n\n## Mobile Long Image Design Defaults\n\nUse these defaults unless the user specifies otherwise:\n\n- Design canvas concept: phone-friendly vertical card.\n- CSS content width: `720px` to `900px`.\n- Recommended default: `width: 780px` for the main card.\n- Body padding: `32px` to `56px`.\n- Export width setting on website: usually `auto`.\n- Export height setting on website: always prefer `auto` for long images.\n- Export scale: always set `Scale = 4` unless performance fails.\n- Final PNG effective pixel width:\n  - If CSS width is `780px` and Scale is `4`, output is about `3120px` wide.\n  - This keeps long text crisp when viewed on phones.\n- Avoid ultra-wide layouts.\n- Avoid tiny text. Chinese body text should usually be `26px` to `34px` CSS pixels before scaling.\n- Body line-height: usually `1.75` to `2.05` for Chinese long text.\n- Paragraph spacing: usually `20px` to `34px`.\n- Title size: usually `52px` to `88px`, depending on content.\n- Use `box-sizing: border-box` globally.\n- Use local/system fonts first; external fonts may fail due to CORS.\n\nRecommended base CSS skeleton:\n\n```css\n* { box-sizing: border-box; }\nhtml, body { margin: 0; padding: 0; }\nbody {\n  width: 780px;\n  margin: 0;\n  padding: 48px;\n  font-family: \"Noto Serif SC\", \"Source Han Serif SC\", \"Songti SC\", \"Microsoft YaHei\", serif;\n  background: #E0EAF5;\n  color: #2C4563;\n}\n.card {\n  width: 100%;\n  border-radius: 36px;\n  padding: 64px 58px;\n  overflow: hidden;\n}\n```\n\n## Website Workflow\n\nUse the browser package.\n\nThis skill uses a **two-stage workflow**:\n\n1. **Preview / refinement stage** — inject HTML/CSS, update the live iframe preview, then show the current Playwright browser tab or preview state to the user. Do **not** download yet unless the user already explicitly asked for immediate final export.\n2. **Final export stage** — after the user confirms the preview is acceptable, set Scale 4, generate PNG, download locally, and display via local media Markdown.\n\n### Stage 1 — Preview and Refinement\n\n1. Open the page:\n\n```text\nhttps://codeshack.io/html-to-image-converter/\n```\n\n2. Wait until the page loads. Confirm these elements exist:\n\n- `#html-input`\n- `#css-input`\n- `.CodeMirror`\n- `#canvas-width`\n- `#canvas-height`\n- `#filename`\n- `#scale`\n- `#render-target`\n- `.download`\n\n3. Do **not** edit the visible CodeMirror virtual DOM manually. Use the actual CodeMirror instances:\n\n```js\nconst cms = document.querySelectorAll('.CodeMirror');\ncms[0].CodeMirror.setValue(html);\ncms[1].CodeMirror.setValue(css);\n```\n\n4. Set preview controls. During preview, keep dimensions realistic and mobile-friendly:\n\n```js\ndocument.getElementById('canvas-width').value = 'auto';\ndocument.getElementById('canvas-height').value = 'auto';\ndocument.getElementById('filename').value = filename;\n```\n\nFor preview, Scale does not matter visually inside the iframe, but set it to `4` early so the final state is ready:\n\n```js\nconst scale = document.getElementById('scale');\nscale.value = '4';\nscale.dispatchEvent(new Event('input', { bubbles: true }));\n```\n\n5. Let preview update, then verify iframe content:\n\n```js\nconst iframe = document.getElementById('render-target');\nconst node = iframe.contentDocument.documentElement;\nreturn {\n  width: node.scrollWidth,\n  height: node.scrollHeight,\n  text: iframe.contentDocument.body.innerText.slice(0, 300)\n};\n```\n\n6. Show the user the current Playwright browser tab / online preview state before downloading. Use `browser:snapshot` if a structural check is enough, or use a screenshot/browser preview method if available in the environment. Tell the user clearly:\n\n```text\n哥哥，我先不下载，预览已经更新了。你看这个在线预览，如果需要精修，我继续改；如果可以，我再 Scale 4 下载。\n```\n\n7. If the user requests changes, modify HTML/CSS and repeat Stage 1. Do not download during iterative refinement.\n\n### Stage 2 — Final Export After Confirmation\n\nOnly start final export when the user says something equivalent to:\n\n- \"可以，下载\"\n- \"就这样\"\n- \"导出吧\"\n- \"没问题\"\n- \"保存给我\"\n\n8. Generate a direct dataURL using the page's own `htmlToImage.toPng`:\n\n```js\nconst iframe = document.getElementById('render-target');\nconst node = iframe.contentDocument.documentElement;\nconst dataUrl = await htmlToImage.toPng(node, {\n  pixelRatio: 4,\n  backgroundColor: 'transparent',\n  width: node.scrollWidth,\n  height: node.scrollHeight\n});\n```\n\n9. Create a temporary direct download link and click it:\n\n```js\nconst old = document.getElementById('yuyan-direct-download');\nif (old) old.remove();\nconst a = document.createElement('a');\na.id = 'yuyan-direct-download';\na.textContent = 'Yuyan Direct Download PNG';\na.href = dataUrl;\na.download = filename;\na.style.cssText = 'display:block;position:fixed;z-index:999999;left:20px;top:20px;background:#2C4563;color:white;padding:14px 18px;border-radius:10px;font-size:16px;';\ndocument.body.appendChild(a);\n```\n\nThen click the temporary link via browser click. This is more reliable than clicking the website's original `Download Image` button in this environment.\n\n10. Confirm the browser download list shows completion and saved path, usually:\n\n```text\n/storage/emulated/0/Download/<filename>.png\n```\n\n11. Display the image using local media Markdown:\n\n```markdown\n![图片说明](file:///storage/emulated/0/Download/<filename>.png)\n```\n\n## Quality Parameters\n\nAlways use:\n\n- File format: PNG.\n- Export scale: `4`.\n- Width: `auto`, unless the user requests exact dimensions.\n- Height: `auto` for long images.\n- Background: transparent at exporter level, but CSS should normally define a visible background to avoid unexpected transparency.\n- Filename: descriptive lowercase filename, e.g. `poem-snow-mountain-poster.png`.\n\nFor long text or poems:\n\n- Use semantic HTML sections.\n- Avoid putting all text in one huge paragraph.\n- Use paragraph tags or grouped stanza blocks.\n- Add visual rhythm: title block, divider, content blocks, footer/signature.\n- Use all user-provided palette colors if requested.\n- Do not silently replace user content with invented content unless asked.\n- If copyright or policy constraints apply, clearly state limitations and offer allowed alternatives, but respect user-provided text when allowed by context.\n\n## Timing Estimate\n\nTypical full workflow duration after this skill is activated:\n\n- Understand content and design direction: 30–90 seconds.\n- Write HTML/CSS: 2–6 minutes for normal cards; 6–15 minutes for complex long poems or dense infographics.\n- Open page and inject code: 20–45 seconds.\n- Set controls and preview check: 20–60 seconds.\n- Generate Scale 4 PNG: 15–90 seconds depending on image height and effects.\n- Download and display: 10–30 seconds.\n\nEstimated total:\n\n- Simple quote/card: about 3–5 minutes.\n- Medium mobile long card: about 5–8 minutes.\n- Complex long poster with carefully styled text: about 8–15 minutes.\n- Very long poem/article with heavy styling: about 12–25 minutes.\n\nWhen the image height is very large, Scale 4 can become slow or memory-heavy. If generation fails, reduce CSS complexity first. Only reduce Scale from 4 to 3 if absolutely necessary and tell the user.\n\n## Failure Modes and Fixes\n\nIf CodeMirror does not update:\n\n- Do not use `innerText` on `.cm-content` or visible editor nodes.\n- Use `.CodeMirror[i].CodeMirror.setValue(...)`.\n\nIf download button does not produce a captured file:\n\n- Generate `dataUrl` manually with `htmlToImage.toPng`.\n- Create a temporary `<a download>` link.\n- Click the temporary link.\n\nIf image is blurry:\n\n- Confirm `#scale.value === '4'`.\n- Use CSS width around `720px–900px`; do not make CSS width too tiny.\n- Increase body font size.\n\nIf image is cropped:\n\n- Use width and height as `auto`.\n- Ensure `node.scrollHeight` is passed to `htmlToImage.toPng`.\n- Avoid fixed `height` on body or card unless intentionally clipping.\n\nIf the image is too huge or generation crashes:\n\n- Simplify shadows, blur, huge gradients, and filters.\n- Split content into multiple images if needed.\n- Try Scale 3 only after explaining the tradeoff.\n\n## Final Response Format\n\nWhen the image is complete, respond with:\n\n- A short completion note.\n- The saved local file path.\n- The key export parameters: CSS width, preview size, scale, approximate output pixels.\n- The local media Markdown image.\n\nExample:\n\n```markdown\n哥哥，做好了~\n\n- 保存路径：`/storage/emulated/0/Download/example-card.png`\n- 导出参数：Scale 4，auto height，CSS 宽度 780px\n- 实际导出约：3120px × 9000px\n\n![example card](file:///storage/emulated/0/Download/example-card.png)\n```